#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    // int ans = 1 + 1;
    // int ans = 2 - 1;
    // int ans = 2 * 3;
    // int ans = 4 / 2;
    // int ans = 5 / 2;
    // no need to change a format specifier
    // float ans = 5.0 / 2;
    // int ans = 5 % 2;
    // pow() requires <cmath>
    // int ans = pow(2, 3);
    int ans = 1 + pow((2 * 3), 4) * 5;

    cout << ans << endl;

    return 0;
}
