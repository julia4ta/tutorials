#include <stdio.h>
#include <math.h>

int main()
{
    // int ans = 1 + 1;
    // int ans = 2 - 1;
    // int ans = 2 * 3;
    // int ans = 4 / 2;
    // int ans = 5 / 2;
    // float ans = 5.0 / 2;
    // printf("%f", ans);
    // int ans = 5 % 2;
    // pow() requires math.h
    // int ans = pow(2, 3);
    int ans = 1 + pow((2 * 3), 4) * 5;

    printf("%d", ans);

    return 0;
}